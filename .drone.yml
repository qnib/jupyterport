workspace:
  base: /go/src
  path: github.com/qnib/jupyterport

pipeline:
  x86:
      image: golang
      commands:
        - mkdir -p bin/v0.1.8
        - go build -o bin/v0.1.8/jupyterport_x86
  #alpine:
  #  image: golang:alpine
  #  commands:
  #  - mkdir -p bin/v0.1.8
  #  - go build -o bin/v0.1.8/jupyterport_alpine
